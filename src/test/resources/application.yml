spring:
  docker:
    compose:
      skip:
        in-tests: false
  cloud:
    aws:
      credentials:
        access-key: xxx
        secret-key: yyy
logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.jdbc.core: DEBUG
