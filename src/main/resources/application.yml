spring:
  application:
    name: todoApp
  datasource:
    url: jdbc:postgresql://localhost/tododb
    username: postgres
    password: postgres

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true